# swati 

Swati isolates the complexity of multi-machine(ai) communication into a single **choreographic** script(ts). rather than programming endpoints to listen for specific triggers, you define the entire conversation top-down.

**WIP**

🚧
